"""
app/services — service layer package.

Services contain business logic and orchestrate between routes and repositories.
They have no HTTP concerns (no Request/Response objects).
Services will be populated as features are built.
"""
